Logged in as <%= current_user.name %>
Current Email: <%= current_user.email %>
<%= link_to "Logout", logout_path, method: "destroy" if current_user %>
See something that doesn't look right? <%= link_to "Edit Profile", edit_user_path(current_user) %>
<%= link_to "View Accessories", admin_bike_shop_path %>

<% @orders.each do |order| %>
  Order #<%= order.id %>
<% end %>

<%= link_to "Ordered", admin_dashboard_path %>: <%= @orders.ordered.count %>
<%= link_to "Cancelled", admin_dashboard_path %>: <%= @orders.cancelled.count %>
<%= link_to "Paid", admin_dashboard_path %>: <%= @orders.paid.count %>
<%= link_to "Completed", admin_dashboard_path %>: <%= @orders.completed.count %>
